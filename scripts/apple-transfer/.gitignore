# The map holds user identifiers and emails, and is regenerated by `collect`.
apple-transfer-map.json
# Never let a service account key or an Apple signing key near the repo.
*.json.key
*.p8
service-account*.json
node_modules/
